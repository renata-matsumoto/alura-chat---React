[allowlist]
description = "Supabase secrets moved to environment variables"
commits = [
  "aeabdaf", # Move hardcoded Supabase credentials to environment variables
  "a4800d3", # Prior commit with hardcoded secrets - now resolved
]
paths = [
  ".env.local",
  ".env.example"
]
regexes = [
  '''SUPABASE_ANON_KEY''',
  '''SUPABASE_URL'''
]
